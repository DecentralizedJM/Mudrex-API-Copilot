name: Test

# DISABLED - Railway deploys independently
# Uncomment if you want CI checks
on:
  workflow_dispatch:  # Only run manually, not on push

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Skip
        run: echo "GitHub Actions disabled - Railway handles deployment"
